# Netlify API rewrites — deploy this file alongside the frontend
# When the frontend is hosted on Netlify, this redirects /api/* to the backend
/api/*  https://mainsite.fundocareer.com/api/:splat  200
